# esbuild and workerd ship platform-specific binaries via their postinstall
# scripts, which pnpm blocks by default. Astro's bundler needs esbuild, and
# Wrangler needs workerd for local emulation and deploys, so allow both.
allowBuilds:
  esbuild: true
  workerd: true
